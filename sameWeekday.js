const assert = require('assert');
var sameWeekday = function(date1, date2) {
    const date = new Date('2016-01-01', '2016-03-01');

    var differentDates = date1,
        date2;

    return differentDates

};
assert.equal(sameWeekday('01', '01'), true);
